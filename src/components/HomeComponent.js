import React from "react";

const HomeComponent = () => {
  return (
    <div>
      <p></p>
      {/* <button onclick={() => dispatch(showAllProducts())}>CALL Filtered Products</button> */}
    </div>
  );
};

export default HomeComponent;
